/**
 *
 * @param {Participant} participant
 * @returns
 */
class Player extends _Actor {
  /**
   *
   * @param {Participant} participant
   */
  constructor(participant) {
    super(participant);
  }
}
